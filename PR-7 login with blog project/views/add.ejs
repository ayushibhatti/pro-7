<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Panel</title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet">

    <style>
        /* General Reset */
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Arial', sans-serif;
            background-color: #f4f6f9;
            color: #333;
            background-image: url('../uploads/2.jpg');
            background-repeat: no-repeat;
            background-size: cover;
        }

        /* Sidebar */
        .sidebar {
            height: 100%;
            width: 250px;
            position: fixed;
            top: 0;
            left: -250px; /* Initially hidden */
            background: rgba(0, 0, 0, 0.8);
            overflow-x: hidden;
            transition: 0.4s ease-in-out;
            box-shadow: 2px 0 5px rgba(0, 0, 0, 0.2);
            padding: 20px 0;
        }

        .sidebar.active {
            left: 0;
        }

        .sidebar h1 {
            color: white;
            text-align: center;
            margin-bottom: 30px;
        }

        .nav-links {
            display: flex;
            flex-direction: column;
            gap: 1.5rem;
            padding: 0 20px;
        }

        .nav-link {
            font-size: 1rem;
            padding: 0.8rem 1rem;
            background: rgba(255, 255, 255, 0.2);
            border-radius: 6px;
            display: flex;
            align-items: center;
            gap: 0.5rem;
            color: white;
            transition: all 0.3s ease;
        }

        .nav-link:hover {
            background: rgba(255, 255, 255, 0.4);
        }

        .nav-link i {
            font-size: 1.2rem;
        }

        /* Sidebar Toggle Button */
        .toggle-btn {
            position: fixed;
            top: 20px;
            left: 20px;
            background-color: #4CAF50;
            color: white;
            border: none;
            padding: 10px 15px;
            font-size: 1rem;
            border-radius: 5px;
            cursor: pointer;
            z-index: 1000;
        }

        .toggle-btn:hover {
            background-color: #45A049;
        }

        /* Main Content */
        .main-content {
            margin-left: 260px;
            padding: 2rem;
            transition: margin-left 0.4s ease-in-out;
        }

        .main-content.collapsed {
            margin-left: 0;
        }

        .card {
            background: rgba(255, 255, 255, 0.2); /* Semi-transparent white */
            border-radius: 16px;
            box-shadow: 0 8px 32px rgba(31, 38, 135, 0.37); /* Subtle shadow */
            backdrop-filter: blur(10px); /* Frosted glass blur */
            -webkit-backdrop-filter: blur(10px); /* For Safari support */
            border: 1px solid rgba(255, 255, 255, 0.18); /* Light border */
            overflow: hidden;
            padding: 30px;
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }

        .card:hover {
            transform: translateY(-5px);
            box-shadow: 0 12px 24px rgba(31, 38, 135, 0.4); /* Enhance hover effect */
        }
    </style>
</head>

<body>

    <!-- Sidebar -->
    <button class="toggle-btn" onclick="toggleSidebar()">☰ Menu</button>
    <aside class="sidebar">
        <h1>Add Blogs</h1>
        <div class="nav-links">
            <a href="/view" class="nav-link"><i class="bi bi-view-list"></i> View Blogs</a>
            <a href="/add" class="nav-link"><i class="bi bi-pencil"></i> Add Blog</a>
            <a href="/logout" class="nav-link"><i class="bi bi-box-arrow-right"></i> Log out</a>
        </div>
    </aside>

    <!-- Main Content -->
    <main class="main-content">
        <div class="card mx-auto" style="max-width: 600px; margin: 30px 0;">
            <form action="/addblouges" method="post" enctype="multipart/form-data">
                <div class="mb-4">
                    <label for="name" class="form-label text-white">Blog Name:</label>
                    <input type="text" class="form-control" id="name" name="name" required>
                </div>
                <div class="mb-4">
                    <label for="description" class="form-label text-white">Blog Description:</label>
                    <textarea class="form-control" id="description" name="description" rows="4" required></textarea>
                </div>
                <div class="mb-4">
                    <label for="image" class="form-label text-white">Upload Image:</label>
                    <input type="file" class="form-control" id="image" name="image" required>
                </div>
                <div class="d-grid">
                    <button type="submit" class="btn btn-primary">Submit Blog</button>
                </div>
            </form>
        </div>
    </main>

    <!-- Bootstrap Icons -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons/font/bootstrap-icons.css" rel="stylesheet">

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>

    <!-- JavaScript for Sidebar -->
    <script>
        function toggleSidebar() {
            const sidebar = document.querySelector('.sidebar');
            const content = document.querySelector('.main-content');
            sidebar.classList.toggle('active');
            content.classList.toggle('collapsed');
        }
    </script>
</body>

</html>
